# Tessera - Clippy linting configuration

# Strictness
msrv = "1.97"  # Matches the tested package rust-version

# Deny these lints
# (enforced by the local repository gate)
warn-on-all-wildcard-imports = true
