[build]
rustflags = ["-D", "warnings"]  # Keep the local repository gate warning-free

# Keep builds portable across contributor machines; opt in to native tuning locally.
# Users can add it locally for performance if needed
